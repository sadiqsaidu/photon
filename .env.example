# Solana JSON-RPC (Helius free tier)
RPC_URL=https://mainnet.helius-rpc.com/?api-key=YOUR_KEY

# Primary Yellowstone / Geyser gRPC — SolInfra (pay-as-you-go).
# Use the region host with the https:// scheme (port 443 is implied).
GRPC_URL=https://fra.grpc.solinfra.dev
GRPC_TOKEN=YOUR_SOLINFRA_API_KEY

# Optional second gRPC provider — Helius Yellowstone/LaserStream.
# When set, MultiStream races both providers and takes the first event
# (first-event-wins dedup, per-provider reconnects, staleness watchdog).
# Leave empty to run single-provider; everything still works.
GRPC_URL_2=
GRPC_TOKEN_2=

# Jito Block Engines, comma-separated. sendBundle fans out to all of them and
# the first success wins (429s put an engine on a 1.5 s cooldown).
JITO_ENGINES=https://frankfurt.mainnet.block-engine.jito.wtf,https://amsterdam.mainnet.block-engine.jito.wtf,https://london.mainnet.block-engine.jito.wtf

# LLM for the agent (single model). Pick ONE provider:
#
# Option A — OpenRouter (free models available). If OPENROUTER_API_KEY is set it is used.
OPENROUTER_API_KEY=
OPENROUTER_MODEL=meta-llama/llama-3.3-70b-instruct:free
#
# Option B — Gemini (used only if OPENROUTER_API_KEY is empty).
GEMINI_API_KEY=
GEMINI_MODEL=gemini-2.0-flash

# Account whose transactions we follow to validate the lifecycle tracker.
# Defaults to a Jito tip account (steady, explorer-verifiable). Higher volume = more GB streamed.
WATCH_ACCOUNT=96gYZGLnJYVFmbjzopPSU6QiEV5fGqZNyN9nmNhvrZU5

# Public key only — used by `npm run construct` to build an unsigned bundle. No private key.
WALLET_PUBKEY=

# Optional THROWAWAY secret key for local `npm run submit` / `npm run fault` testing only.
# Never your real wallet. Production signing happens client-side via the dashboard wallet.
WALLET_SECRET=

# Postgres
DATABASE_URL=postgres://photon:photon@localhost:5432/photon

# Tip policy ceiling (lamports)
TIP_CEILING_LAMPORTS=200000

# API server port (serve mode)
PORT=8080
